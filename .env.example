VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Set to "true" to bypass real Supabase phone-auth and log in with any
# 10-digit number + the fixed code 123456, as the mock CHO persona
# (Ashok Kumar). Useful for demos before a Supabase project is wired up.
# Leave unset ("false") once real Supabase phone auth is configured.
VITE_DEV_LOGIN=false
